{
    "name": "scm/schema-control-pro",
    "description": "Schema Control Pro WordPress plugin",
    "type": "wordpress-plugin",
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "optimize-autoloader": true
    }
}
